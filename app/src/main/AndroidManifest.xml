<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kamel.thuglifegame">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.GameActivity" />
        <activity android:name=".Activities.BankActivity" />
        <activity android:name=".Activities.ShopActivity" />
        <activity android:name=".Activities.DealerActivity" />
        <activity android:name=".Activities.HospitalActivity" />
        <activity android:name=".Activities.QuestActivity" />
        <activity android:name=".Activities.CasinoActivity" />
        <activity android:name=".Activities.PrisonActivity" />
        <activity android:name=".Activities.VIPActivity" />
        <activity android:name=".Activities.WhoresActivity" />
        <activity android:name=".Activities.HscoreActivity" />
        <activity android:name=".Activities.AchieveActivity" />
        <activity android:name=".Activities.StatisticActivity" />
        <activity android:name=".Activities.DiceGameActivity"></activity>
    </application>

</manifest>